import { test, expect, Page, Locator } from '@playwright/test';

test.beforeEach(() => {});

test.describe('Accounts', () => {
	test.fixme('create account', () => {});
	test.fixme('edit account', () => {});
	test.fixme('delete account', () => {});
});
